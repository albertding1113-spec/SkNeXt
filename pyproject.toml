[project]
name = "sknext"
version = "0.1.0"
description = "ConvNeXt-V2 based segmentation for biological microscopy images"
requires-python = ">=3.14"
dependencies = [
    "pyyaml",
    "yacs",
]

[project.scripts]
sknext = "sknext:main"